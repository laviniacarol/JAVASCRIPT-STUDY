let idade = 16;
let mensagem;

idade >= 18 ? mensagem = 'Você é maior de idade' : mensagem = 'Você é menor de idade.';
console.log(mensagem);


//teste
let carro = "fiat";
let resultado;
  carro === "porsche" ? resultado = "fast and pretty" :
  carro === "ferrari" ?  resultado = "rich as hell" :
  carro === "fiat" ? resultado = "italiano de luxo" :  
  "I probably forget about it..."  
console.log(resultado);
